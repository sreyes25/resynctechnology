---
const bullets = [
  { title: "NYC‑based engineering", desc: "A lean, fast-moving software team built in New York City." },
  { title: "Founder-led execution", desc: "You work directly with the engineer building your product — no layers or handoffs." },
  { title: "Automation & reliability", desc: "We eliminate repetitive work and ship systems that scale without breaking." },
  { title: "Clarity & transparency", desc: "Clear milestones, predictable timelines, and visibility into progress." },
];
---

<section id="why" class="relative overflow-hidden border-t border-white/10 bg-[#0b0f14] py-16 text-white">
  <!-- Ambient background (subtle, premium) -->
  <div class="pointer-events-none absolute inset-0">
    <div class="absolute inset-0 bg-[radial-gradient(60%_80%_at_18%_12%,rgba(99,102,241,0.22),transparent_60%),radial-gradient(60%_80%_at_82%_18%,rgba(16,185,129,0.18),transparent_58%),radial-gradient(70%_90%_at_60%_110%,rgba(255,255,255,0.05),transparent_55%)]"></div>
    <div class="absolute inset-0 opacity-25 [background-image:radial-gradient(circle_at_1px_1px,rgba(255,255,255,0.10)_1px,transparent_1px)] [background-size:8px_8px]"></div>
    <div class="absolute inset-0 bg-[radial-gradient(120%_90%_at_50%_0%,transparent_45%,rgba(0,0,0,0.65)_100%)]"></div>
  </div>

  <div class="relative mx-auto max-w-6xl px-4 sm:px-6">
    <div class="grid gap-10 md:grid-cols-12 md:items-start">
      <!-- Left: heading / copy -->
      <div class="md:col-span-5">
        <div class="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/5 px-3 py-1 text-xs text-white/80 backdrop-blur">
          <span class="h-2.5 w-2.5 rounded-full bg-resync-500"></span>
          <span class="font-semibold">Why Resync</span>
          <span class="h-3 w-px bg-white/15"></span>
          <span class="font-mono text-[11px] tracking-wider text-white/65">NYC</span>
        </div>

        <h2 class="mt-5 text-3xl font-semibold tracking-tight sm:text-4xl">
          Engineering-led delivery with a founder’s level of care.
        </h2>

        <p class="mt-3 text-sm leading-relaxed text-white/70 sm:text-base">
          We build modern apps, websites, and automation with tight feedback loops, clean architecture, and measurable results — without the agency bloat.
        </p>

        <!-- Proof chips -->
        <div class="mt-6 flex flex-wrap gap-2">
          <span class="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-3 py-1 text-xs text-white/75">
            <span class="h-1.5 w-1.5 rounded-full bg-emerald-400"></span>
            Fast iteration
          </span>
          <span class="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-3 py-1 text-xs text-white/75">
            <span class="h-1.5 w-1.5 rounded-full bg-violet-400"></span>
            Clean, maintainable code
          </span>
          <span class="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-3 py-1 text-xs text-white/75">
            <span class="h-1.5 w-1.5 rounded-full bg-white/60"></span>
            Clear milestones
          </span>
        </div>
      </div>

      <!-- Right: feature cards -->
      <div class="md:col-span-7">
        <div class="grid gap-4 sm:grid-cols-2">
          {bullets.map((b, i) => (
            <div class="group relative overflow-hidden rounded-2xl border border-white/10 bg-white/[0.04] p-6 shadow-soft transition-all duration-200 hover:-translate-y-0.5 hover:border-white/20 hover:bg-white/[0.06]">
              <!-- Card glow -->
              <div class="pointer-events-none absolute -inset-10 opacity-0 blur-2xl transition-opacity duration-300 group-hover:opacity-100 bg-[radial-gradient(circle_at_30%_20%,rgba(255,255,255,0.16),transparent_60%)]"></div>
              <!-- Accent strip -->
              <div class="absolute left-0 top-0 h-full w-px bg-[linear-gradient(180deg,rgba(99,102,241,0.55),rgba(16,185,129,0.45),transparent)] opacity-80"></div>

              <div class="relative">
                <div class="flex items-start justify-between gap-4">
                  <div>
                    <div class="text-sm font-semibold tracking-tight text-white">
                      {b.title}
                    </div>
                    <div class="mt-2 text-sm leading-relaxed text-white/70">
                      {b.desc}
                    </div>
                  </div>

                  <!-- Minimal index badge (subtle) -->
                  <span class="inline-flex h-9 w-9 shrink-0 items-center justify-center rounded-full border border-white/10 bg-white/5 text-xs font-semibold text-white/70">
                    {String(i + 1).padStart(2, "0")}
                  </span>
                </div>

                <!-- Hover cue -->
                <div class="mt-4 h-px w-full bg-white/10"></div>
                <div class="mt-3 text-xs font-semibold text-white/60 transition-colors group-hover:text-white/80">
                  Built for production — not prototypes.
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>